<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/item_reservation_background"
    android:layout_marginBottom="8dp"
    android:padding="15dp">

    <ImageView
        android:id="@+id/item_reservation_thumbnail"
        android:layout_width="45dp"
        android:layout_height="76dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/item_reservation_title"
        style="@style/TextAppearance.AppCompat.Title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="20dp"
        app:layout_constraintStart_toEndOf="@id/item_reservation_thumbnail"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="The Batman" />

    <TextView
        android:id="@+id/item_reservation_date"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:textColor="@color/black"
        android:layout_marginStart="20dp"
        app:layout_constraintStart_toEndOf="@id/item_reservation_thumbnail"
        app:layout_constrainedWidth="true"
        app:layout_constraintTop_toBottomOf="@id/item_reservation_title"
        tools:text="18 de Abril 2023, 18:00" />

    <TextView
        android:id="@+id/item_reservation_cinema"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="20dp"
        android:layout_marginHorizontal="8dp"
        app:layout_constraintStart_toEndOf="@id/item_reservation_thumbnail"
        app:layout_constrainedWidth="true"
        app:layout_constraintTop_toBottomOf="@id/item_reservation_date"
        tools:text="Cine Hoyts" />

</androidx.constraintlayout.widget.ConstraintLayout>